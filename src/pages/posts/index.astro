---
import Layout from '../../layouts/Layout.astro';
import { getPosts } from '../../lib/content';

const allPosts = await getPosts();
const posts = allPosts
  .filter(post => !post.data.draft)
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf());
---

<Layout title="Blog | Josh Giles">
  <div class="animate-fade-in">
    <!-- Header -->
    <div class="mb-12">
      <p class="section-label flex items-center gap-2 mb-4">
        <span class="w-4 h-px bg-gray-600"></span>
        Blog
      </p>
      <h1 class="text-4xl md:text-5xl font-bold text-white mb-4">
        All Posts
      </h1>
      <p class="text-gray-400">
        {posts.length} {posts.length === 1 ? 'article' : 'articles'} on AI, development, and technology.
      </p>
    </div>

    <!-- Posts List -->
    <div class="space-y-6">
      {posts.map((post, index) => (
        <article
          class="group bg-slate-800 rounded-xl border border-slate-700/50 overflow-hidden hover:border-amber-500/30 transition-all duration-300"
          style={`animation: slideUp 0.6s ease-out ${index * 50}ms backwards`}
        >
          <a href={`/posts/${post.slug}`} class="flex flex-col md:flex-row">
            <!-- Image -->
            {post.data.image ? (
              <div class="md:w-64 lg:w-80 h-48 md:h-auto shrink-0 overflow-hidden">
                <img
                  src={post.data.image}
                  alt={post.data.title}
                  class="w-full h-full object-cover group-hover:scale-105 transition-transform duration-500"
                />
              </div>
            ) : (
              <div class="md:w-64 lg:w-80 h-48 md:h-auto shrink-0 bg-gradient-to-br from-slate-700 to-slate-800 flex items-center justify-center">
                <svg class="w-12 h-12 text-amber-500/20" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M19 20H5a2 2 0 01-2-2V6a2 2 0 012-2h10a2 2 0 012 2v1m2 13a2 2 0 01-2-2V7m2 13a2 2 0 002-2V9a2 2 0 00-2-2h-2m-4-3H9M7 16h6M7 8h6v4H7V8z" />
                </svg>
              </div>
            )}

            <!-- Content -->
            <div class="flex-1 p-6">
              <div class="flex items-center gap-3 mb-3">
                <time
                  datetime={post.data.date.toISOString()}
                  class="text-sm text-gray-500"
                >
                  {post.data.date.toLocaleDateString('en-US', {
                    year: 'numeric',
                    month: 'short',
                    day: 'numeric',
                  })}
                </time>
              </div>

              <h2 class="text-xl md:text-2xl font-semibold text-white group-hover:text-amber-500 transition-colors mb-3">
                {post.data.title}
              </h2>

              <p class="text-gray-400 mb-4 line-clamp-2">
                {post.data.description}
              </p>

              <!-- Tags -->
              <div class="flex flex-wrap gap-2">
                {post.data.tags.map((tag) => (
                  <span class="text-xs px-3 py-1 rounded-full bg-slate-700/50 text-gray-400 border border-slate-600/50">
                    #{tag}
                  </span>
                ))}
              </div>
            </div>

            <!-- Arrow -->
            <div class="hidden md:flex items-center px-6 text-amber-500 opacity-0 group-hover:opacity-100 transition-opacity">
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
              </svg>
            </div>
          </a>
        </article>
      ))}
    </div>

    {posts.length === 0 && (
      <div class="text-center py-20">
        <p class="text-gray-500">No posts yet. Check back soon!</p>
      </div>
    )}
  </div>
</Layout>

<style>
  @keyframes slideUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>
